// const moderationLogChannel = message.guild?.channels.cache.find(channel => channel.type === ChannelType.GuildText && channel.name === 'moderation-log') as TextChannel;
// moderationLogChannel?.send(`User ${member.user.toString()} (${member.user.id}) has been muted for spamming.`);


// function ( how do what, file(optional))
